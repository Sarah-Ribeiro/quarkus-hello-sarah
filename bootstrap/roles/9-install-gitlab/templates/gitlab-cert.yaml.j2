apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cert-gitlab
  namespace: "{{ gitlab_namespace }}"
spec:
  isCA: false
  commonName: gitlab
  secretName: sct-gitlab-tls
  dnsNames:
    - "apps.cluster-2l9jd.sandbox628.opentlc.com"
    - "*.apps.cluster-2l9jd.sandbox628.opentlc.com"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging


